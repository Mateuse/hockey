import { SimpleGoaliePipe } from './simple-goalie.pipe';

describe('SimpleGoaliePipe', () => {
  it('create an instance', () => {
    const pipe = new SimpleGoaliePipe();
    expect(pipe).toBeTruthy();
  });
});
