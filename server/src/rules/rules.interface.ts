export interface PointRules{
    forwards: JSON,
    defense: JSON,
    goalies: JSON,
    teams: JSON
}

export interface PositionRules{
    centers: Number,
    wingers: Number,
    defense: Number,
    goalies: Number,
    teams: Number,
    wild: Number
}